language: python

python:
    - 3.3
    - 3.4
sudo: required
before-install:
    - sudo apt-get update
install: 
    - bash virtualenv-bootstrap.sh  #cause of travis' setup, we will already be in a virtualenv and reuse it
script:
    - LD_LIBRARY_PATH="$VIRTUAL_ENV/lib/:$LD_LIBRARY_PATH" ./test.sh #cause of travis' setup, we will already be in a virtualenv and reuse it, little hack to get LD_LIBRARY_PATH right
