version: "2"
services:
  lamachine:
    from: "debian:stable"
    roles:
      - role: andrewrothstein.conda-env
        conda_env_name: lamachine
	    conda_env_environment: conda-base-environment.yml
    ports:
      - "80:80"
    #command: ["/usr/bin/dumb-init", "/usr/sbin/apache2ctl", "-D", "FOREGROUND"]
    #dev_overrides:
    #  environment:
    #    - "DEBUG=1"
