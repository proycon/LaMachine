version: "2"
services:
  LM_NAME:
    from: "debian:stable"
    roles: #this lists the packages to install in your container
#ROLES_PLACEHOLDER
    ports:
      - "8080:80" #make sure this corresponds exactly with the values you previously selected for mapped_http_port and http_port, respectively!
    #command: ["/usr/bin/dumb-init", "/usr/sbin/apache2ctl", "-D", "FOREGROUND"]
    #dev_overrides:
    #  environment:
    #    - "DEBUG=1"
